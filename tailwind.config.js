const { orange } = require('tailwindcss/colors');

module.exports = {
  theme: {
    extend: {
      colors: { orange },

      fontFamily: {
        roboto: ['Roboto', 'sans-serif'],
      },
    },
  },

  variants: {
    extend: {},
  },
}
